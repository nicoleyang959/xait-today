# xAIT 今日智能体协作约定

本文件适用于 Codex、Claude Code、Gemini CLI 及其他自动化开发工具。不要依赖某个智能体专属的隐藏配置；所有必要操作都必须能够用仓库内文件和 Python 标准库完成。

## 标准流程

在仓库根目录运行：

```bash
python3 xait.py check
python3 xait.py build
python3 xait.py serve
```

Windows 使用 `py -3` 代替 `python3`。默认地址是 `http://127.0.0.1:8022/`。只有用户明确需要局域网访问时，才使用 `python3 xait.py serve --lan`。

上述命令也必须支持从仓库外通过 `python3 /path/to/xait-today/xait.py ...` 调用。不得要求调用者先切换到特定目录，也不得读取或写入调用者的当前目录。

## 数据和生成边界

- `content/issues/YYYY-MM-DD.json` 是唯一规范内容输入。
- `site/` 保存模板、样式和交互源码。
- `docs/` 是可直接部署的确定性构建产物。
- `outputs/` 是公开研究快照，不得作为未声明的构建输入。
- 只能归档真实存在且通过校验的 issue；不得补造日期或内容。
- 修改规范输入或页面源码后必须重新运行 `build`，并提交相应的 `docs/` 产物。
- 相同源码和输入必须产生逐字节一致的构建结果。
- 构建必须先在临时目录完成，全部校验通过后才能替换 `docs/`。

## 安全与隐私

仓库是公开的。不得读取、复制、输出或提交：

- Cookie、密码、令牌、API 密钥或浏览器存储；
- 数据库、缓存、日志、原始采集响应或登录态文件；
- 用户主目录、绝对本机路径、局域网地址或设备标识；
- 公众号后台数据或不可公开核验的阅读量。

公开链接只允许 `http://` 或 `https://`，且不得指向回环、局域网、链路本地或包含凭据的地址。不得把 HTML、脚本或 Markdown 当作 issue 中可执行的页面内容；渲染时必须转义所有数据文本。

仓库不包含私有采集器。不要寻找、猜测或调用作者电脑上的采集脚本和登录态服务。无法采集新数据时，应继续使用仓库中最近一次成功的公开 issue，并保留其真实日期与来源状态，不得伪装成当天数据。

## 兼容性

- 运行时最低版本为 Python 3.9。
- 仅使用 Python 标准库；不要引入 npm、Docker 或第三方 Python 依赖作为必需步骤。
- 兼容 macOS、Linux 和 Windows。
- 页面不得依赖远程字体、构建 CDN 或本机文件。
- 保持七套主题、主题记忆、历史侧栏、侧栏收缩和响应式布局正常。
- 外部链接须使用安全的新窗口属性。

## 修改后的检查

至少运行：

```bash
python3 -m unittest discover -s tests -v
python3 xait.py check
python3 xait.py build
git diff --exit-code
```

最后一个命令用于确认已提交的生成文件与源码一致；开发中的预期改动可以先查看差异，再在完成时统一提交。不得通过关闭检查、删除失败用例或放宽敏感信息规则来让 CI 通过。

如果端口冒烟测试需要启动服务，应使用随机可用端口，并在测试结束后可靠地关闭进程。
